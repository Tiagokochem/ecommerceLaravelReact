# Docker Compose — copie para `.env` na raiz: cp .env.example .env
# Usado para portas do host, credenciais do MySQL no compose e URL da API no Vite.

DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=secret
DB_PORT=3307
APP_PORT=8000
FRONTEND_PORT=5174
VITE_API_URL=http://localhost:8000
